# This script runs each time the computer starts up or when the user wants to manually start the keyboard driver

# They handle for MacOS detecting the correct serial port and launching the main executable with the right parameters