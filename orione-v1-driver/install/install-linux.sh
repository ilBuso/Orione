# This script runs only once when the user first sets up your keyboard driver

# Handles for linux tasks like:
# - Copying the executable and scripts to the correct locations
# - Setting up permissions (especially important for serial ports on Linux)
# - Creating system services or startup tasks so your driver launches automatically
# - Installing any dependencies if needed